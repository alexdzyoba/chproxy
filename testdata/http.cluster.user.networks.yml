server:
  http:
    listen_addr: ":9090"

users:
  - name: "default"
    to_cluster: "default"
    to_user: "web"

clusters:
  - name: "default"
    nodes: ["127.0.0.1:8124"]
    users:
      - name: "web"
